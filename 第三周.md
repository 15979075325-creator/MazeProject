# 第 3 周计划（成员B）：A* 算法 + 单测

## 本周目标

新增 `aStarFind`，与 BFS/Dijkstra 形成三算法对照，单测断言三者路径长度一致。

> 注：本周"终端可视化 + 路径动画"是成员A的任务，成员B 只需保证求解器接口稳定供其调用。

---

## 任务1：新建 `include/solver/AStar.h`

接口与 `Solver.h` 风格一致：

```cpp
#pragma once
#include "core/Common.h"
#include <vector>

// A* 求最短路径：优先队列 + 曼哈顿启发式 f = g + h
std::vector<Pos> aStarFind(const Grid& grid, Pos start, Pos goal);
```

## 任务2：实现 `src/solver/AStar.cpp`

可复用 `Solver.cpp` 的骨架（注意 `inBounds/isOpen/DR/DC` 目前是 `static`，建议提到公共位置或复制一份）：

1. **合法性检查**：同 `bfsFind`，起点/终点越界或为墙 → 返回空
2. **数据结构**：
   - `gScore` 二维数组，`INT_MAX/2` 初始化，起点为 0
   - `cameFrom` 二维 `Pos` 记录前驱
   - 最小堆元素 `(f, g, pos)`——堆按 `f` 排序，`f` 相同用 `g` 大者优先（更接近终点，减少扩展）
3. **启发式**：`h = |r - goal_r| + |c - goal_c|`（曼哈顿距离，四方向移动下可采纳，保证最优）
4. **主循环**：
   - 弹出 `(f, g, cur)`，若 `g != gScore[cur]` 跳过过期节点
   - `cur == goal` → 提前终止
   - 松弛四邻居：`ng = g + 1`，若 `ng < gScore[nb]` 则更新 `cameFrom`，压入 `(ng + h(nb), ng, nb)`
5. **回溯**：同 BFS，沿 `cameFrom` 回溯到起点后 `std::reverse`

## 任务3：扩展 `tests/test_solver.cpp`

- 补 3 个用例（直线、L 形、无解各调一次 `aStarFind`）
- 关键断言：**三算法路径长度一致**，且首元素 = start、末元素 = goal
- 顺手补全 `pathValid` 的 TODO：相邻步曼哈顿距离 == 1、每步不是墙

## 任务4：更新 `build.bat`

- 两个编译目标都追加 `src/solver/AStar.cpp`（test_solver 与 demo_b）
- demo_b 可加一行对比输出：A* 路径长度 == BFS 路径长度

## 任务5：联调准备 + 提交

- 若成员A的 `Utils::rng()` 已合并：把 `DfsGenerator.cpp` 里的局部 `rng()` 换成它（第2周遗留项），保证同种子可复现
- 编写本周工作记录，提交推送到 `feature/member-b`

---

## 执行顺序与验收对照

| 步骤 | 产出 | 验收 |
|:--:|------|------|
| 1 | AStar.h/.cpp 骨架 | 编译通过 |
| 2 | aStarFind 实现 | 手写小迷宫跑通 |
| 3 | 测试扩展 | `test_solver.exe` 全 PASS，三算法等长 |
| 4 | build.bat + demo | `demo_b.exe` 输出一致 |
| 5 | 换 rng + 提交推送 | 远程分支更新 |

## 本周验收清单（对照项目计划）

- [ ] A*/BFS/Dijkstra 路径长度一致（单测佐证）
- [ ] `build.bat` 编译 0 错误
- [ ] 接口稳定，可配合成员A的终端动画演示
