# 迷宫求解与可视化 — 7周团队开发计划

## 一、项目概述

- **运行语言**：C++（C++17 标准）
- **开发周期**：7 周
- **团队人数**：2 人（下文称 成员A、成员B）
- **项目目标**：实现一个支持随机生成的迷宫生成器与求解器，并提供可视化界面，逐步扩展多终点、动态障碍物、自定义编辑等高级功能。

### 核心功能
| 模块 | 说明 | 难度 |
|------|------|------|
| 迷宫数据模型 | 二维矩阵，墙壁/通路表示 | 基础 |
| 迷宫生成器 | 随机生成迷宫（多种算法） | 中等 |
| 路径求解器 | 起点到终点的有效路径 | 中等 |
| 路径可视化 | 图形化显示迷宫与路径 | 中等 |
| 多终点支持 | 到达所有终点的最短路径 | 挑战 |
| 动态障碍物 | 实时变化环境下的路径规划 | 挑战 |
| 自定义编辑 | 手动设置起点/终点/障碍物 | 扩展 |

---

## 二、技术选型与架构

### 推荐技术栈
- **构建工具**：直接使用 `g++` 命令行 + 编译脚本（`build.bat` / `build.sh`），**不使用 CMake**（文件量小、无需额外安装，Windows/Mac/Linux 一条命令即可编译）
- **可视化库**：SFML 或 raylib（二者选一，SFML 更常见，文档丰富）
  - > 备选：若不需要图形界面，可用终端字符版 `# + 空格` 可视化，降低上手成本，便于先跑通核心逻辑。
- **单元测试**：GoogleTest（可选，关键路径用）
- **代码管理**：Git + 一个统一远程仓库

### 建议的顶层模块划分
```
maze_project/
├── build.bat        // Windows 一键编译脚本（g++）
├── include/
│   ├── model/        // 迷宫数据结构、格子类型
│   ├── generator/    // 迷宫生成算法
│   ├── solver/       // 路径求解算法
│   ├── visual/       // 渲染与交互
│   └── core/         // 通用工具（计时、随机数、文件IO）
├── src/
│   ├── model/
│   ├── generator/
│   ├── solver/
│   ├── visual/
│   └── core/
├── tests/
└── docs/
```

---

## 三、模块与分工

> 原则：二人各自承担**一个生成模块 + 一个算法模块 + 一个核心模块**，保证前期能并行推进、后期在可视化处汇合集成。

| 模块 | 内容 | 负责人 |
|------|------|:------:|
| 迷宫数据模型 | Grid/Maze 类：二维矩阵、格子状态（墙/路/起点/终点）、读写、边界判断 | 成员A |
| 迷宫生成器 | 基于 DFS(递归回溯)、Kruskal 或 Prim 的随机迷宫生成 | 成员B |
| 路径求解器 | BFS（最短步数）、A*（带启发式）、Dijkstra（加权） | 成员B |
| 路径可视化 | 图形渲染（SFML/raylib 或终端字符）、路径高亮动画 | 成员A |
| 通用核心 | 随机数种子、耗时统计、迷宫文件序列化、命令行参数 | 成员A |
| 高级扩展 | 多终点搜索、动态障碍物实时重规划、手动编辑交互 | 成员A+B 协同 |

---

## 四、7 周详细计划

### 第 1 周：准备与数据模型

> 本周目标：把项目骨架建好，让「一个固定小迷宫能被构建并打印到终端」成为可编译运行的最小程序。这是所有后续功能的底座，务必确保**两台机器都能一键编译运行**。

**影片产出 / 验收**：`git clone` 后执行 `build.bat`（或 `./build.sh`）即可编译，运行 `./maze_app` 能在终端打印一个写死的 9×9 小迷宫。

---

#### 双方协作：搭建项目骨架（编译脚本 + Git）

- **方法**：源码平铺 + 根目录一个编译脚本（g++ 直接输出 `maze_app.exe`），无需 CMake、无需独立构建目录，双方 clone 后双击/执行脚本即可编译。

- 根目录结构（本周先建这一层）：
  ```
  maze_project/
  ├── build.bat          # Windows：g++ 一键编译（sh 版 build.sh 给 Mac/Linux）
  ├── include/
  │   └── model/Maze.h       # 成员A之后放头文件
  ├── src/
  │   ├── model/Maze.cpp     # 成员A之后放实现
  │   └── main.cpp
  ├── tests/                 # 本周可为空，预留
  ├── .gitignore             # 忽略 *.exe
  └── README.md              # 一行编译命令说明
  ```

- **build.bat 内容**（骨架，双方各自 clone 后直接可用）：
  ```bat
  @echo off
  REM 编译所有源文件为 maze_app.exe（.cpp 新增时无需改这里，用通配符收集）
  g++ -std=c++17 -I include -o maze_app.exe src/main.cpp src\model\Maze.cpp
  if errorlevel 1 (echo 编译失败 & exit /b 1)
  echo 编译成功：maze_app.exe
  ```
  > Windows 用 `build.bat`；Mac/Linux 用同内容的 `build.sh`（把 `\` 换 `/`）。后续加了生成器/求解器，只需往 `g++` 命令里追加对应 `.cpp`（或在脚本里用 `src\generator\*.cpp src\solver\*.cpp` 通配收集）。
  > 若某些目录还没有文件，`g++` 通配会报"No such file"，此时可先只写当前存在的文件。

- **编码步骤**：
  - [ ] 创建目录与上述文件，写一个临时 `main.cpp` 打印 `"hello maze"`，先确保能编译。
  - [ ] `.gitignore` 至少含：`*.exe`、`*.out`。
  - [ ] `git init`，提交一次初始版本，推送到统一远程仓库（如 GitHub/Gitee）。
  - [ ] 双方各 `clone` 一遍，各自跑通 `build.bat` 编译，确认环境一致。
  - [ ] **确认可视化库选型**：本周只做决策（默认 `SFML`，备选 `raylib`），并各自安装到本机；安装步骤记录到 README，供第 3 周使用。若本机装图形库困难，第 3 周可用「终端字符可视化」兜底。

---

#### 成员A：Maze 数据模型

- **负责文件**：`include/model/Maze.h`、`src/model/Maze.cpp`

- **选用方法**：面向对象封装，对外只暴露「读迷宫、判断格子、设置格子」的安全接口，内部用 `std::vector<std::vector<char>>` 存数据。

- **推荐接口（头文件）**：
  ```cpp
  class Maze {
  public:
      Maze(int h = 9, int w = 9);        // 默认 9×9
      int height() const;
      int width()  const;
      bool inBounds(int r, int c) const;      // 是否越界
      bool isWall(int r, int c) const;        // 是否墙 ('#')
      void setCell(int r, int c, char ch);    // 设置某个格子
      char cell(int r, int c) const;          // 读取某个格子
      void print() const;                     // 打印到终端
  private:
      int _h, _w;
      std::vector<std::vector<char>> _grid;   // 默认全部填 '#'
  };
  ```
  - **字符约定**（全项目统一，写入接口注释）：
    `'#'` 墙、`' '` 路、`'S'` 起点、`'E'` 终点。

- **编码步骤**：
  - [ ] 构造时把 `_grid` 初始化为 `_h × _w`、全部填 `'#'`。
  - [ ] 实现 `inBounds` / `isWall` / `setCell` / `cell`（注意 `r,c` 合法性检查，越界时报错或安全返回）。
  - [ ] 实现 `print()`：`std::ostringstream` 按行拼接后一次性 `cout`，保证输出整齐；可选加行号/列号便于调试。
  - [ ] 写一个手工迷宫小样例（9×9，四周是墙、留一条折线通路），在 `main` 里验证 `print()`。

---

#### 成员B：接口调研与约定

- **负责文件**：`docs/interface.md`（本周先写文档，不写实现）

- **选用方法**：先「纸上定契约」，避免两周后两个成员写的结构不兼容。调研重点是接口的「统一性」，不是写代码。

- **调研任务**：
  - [ ] 整理 3 种常见迷宫表示法（二维数组、邻接表、四叉树）的优劣，最终选定**统一用二维 `char` 矩阵**作为全项目迷宫数据格式。
  - [ ] 调研 4 类算法各自的输入输出形状：
    - 生成器：输入 `(宽, 高)` → 输出 `Grid`（迷宫矩阵）
    - 求解器：输入 `(Grid, 起点, 终点)` → 输出 `Path`（`vector<pair<int,int>>` 坐标序列）
  - [ ] 定下项目级「契约」文档 `docs/interface.md`，内容至少包括：
    - 坐标类型 `using Pos = std::pair<int,int>;`（r 先行、c 后列）
    - 迷宫矩阵 `Grid = std::vector<std::vector<char>>`
    - 字符约定（与成员A的 `Maze` 一致）
    - 生成器/求解器的函数签名草案（直接采用第 2 周文档里的接口）
  - [ ] 给成员A看这份文档确认，双方意见一致后再进入第 2 周编码。

**本周验收清单**
- [ ] 全新环境 clone 后能 `build.bat` 编译成功、运行打印迷宫。
- [ ] 成员A 的 `Maze` 能构建 9×9 手工迷宫并美观打印。
- [ ] 成员B 的 `docs/interface.md` 已定稿并被两人确认。
- [ ] Git 仓库已建、初始提交已推送，双方都 pull 到最新。

### 第 2 周：生成器 + 求解器基础

> 本周产出：可执行程序 `maze_app`，在终端完成「生成迷宫 → 求解 → 打印路径」的完整闭环。本周代码不依赖任何第三方图形库，只依赖 C++17 标准库，保证人人都能本地跑通；跑通后的迷宫和路径数据即为第 3 周图形可视化模块的输入。

**本周结束时目录结构**（在第 1 周骨架基础上补齐源码）

```
maze_project/
├── build.bat                    # g++ 一键编译脚本（追加新 .cpp 到 g++ 命令）
├── include/
│   ├── model/Maze.h             # 迷宫数据结构（成员A维护）
│   ├── generator/DfsGenerator.h # DFS 生成器接口（成员B）
│   ├── solver/Solver.h          # BFS / Dijkstra 接口（成员B）
│   └── core/Utils.h             # 随机数、计时、打印（成员A）
└── src/
    ├── model/Maze.cpp
    ├── generator/DfsGenerator.cpp
    ├── solver/Solver.cpp
    ├── core/Utils.cpp
    └── main.cpp                 # 双方协作，串联入口
```

---

#### 成员B：DFS 迷宫生成器（递归回溯）

- **负责文件**：`include/generator/DfsGenerator.h`、`src/generator/DfsGenerator.cpp`

- **选用方法**：递归回溯（深度优先）。核心原则是「从起点开始挖路，一次拆一面墙」，保证迷宫是**完美迷宫**——任意两格之间只有唯一通路、整体连通、含少量无意义分支（复杂度控制放到第 4 周）。

- **数据结构**（封装进 `DfsGenerator` 类）：
  - `int width, height`：迷宫行列数，**必须为奇数**（这样外圈完整、路格居中、边界判断简单）。
  - `std::vector<std::vector<char>> grid`：储迷宫，`'#'` 表示墙、`' '` 表示路。

- **推荐开放接口**（成员A在 main 里依赖的"契约"）：
  ```cpp
  class DfsGenerator {
  public:
      DfsGenerator(unsigned w, unsigned h);          // w/h 为奇数
      void generate();                                // 执行生成
      const std::vector<std::vector<char>>& grid() const;
      std::pair<int,int> start() const;               // 起点，固定 (1,1)
      std::pair<int,int> goal()  const;               // 终点，固定 (h-2, w-2)
  private:
      void carve(int r, int c);                       // 递归回溯核心
  };
  ```

- **编码步骤**：
  - [ ] 构造时把 `grid` 全部填 `'#'`。
  - [ ] `generate()`：把内层奇偶格先挖成路的骨架不必做，直接调用 `carve(1,1)` 即可。
  - [ ] `carve(r,c)` 逻辑：
    1. 当前格 `(r,c)` 置为路 `' '`。
    2. 建立四方向数组（上/下/左/右对应的坐标增量），用 `std::shuffle` 随机打乱顺序（随机源用随机数工具的 `rng()`）。
    3. 遍历每个方向 `(dr,dc)`，设「下一格」为 `(r+2*dr, c+2*dc)`（隔一跳，保证墙厚 1）：
       - 若下一格仍在范围内且仍是墙，则先打通两格中间的墙 `grid[r+dr][c+dc]=' '`，再递归 `carve(r+2*dr, c+2*dc)`。
  - [ ] 起点、终点固定为迷宫左上内角 `(1,1)` 与右下内角 `(h-2,w-2)`。
  - [ ] 验证：生成后打印，确认起点到终点有通路、四方向都能走通。

---

#### 成员B：BFS 与 Dijkstra 求解器

- **负责文件**：`include/solver/Solver.h`、`src/solver/Solver.cpp`

- **选用方法**：
  - **BFS**：普通队列 `std::queue<Pos>`。因为迷宫默认每条路代价相同，BFS 天然给出**最短步数**，代码最简单，用 `visited` + `parent` 数组回溯路径。
  - **Dijkstra**：优先队列 `std::priority_queue`（最小堆）。面向后续带「代价」的场景（第 6 周动态障碍物），本周先作为一种与 BFS 结果对拍验证的算法实现出来。

- **推荐开放接口**（共享 `Pos = std::pair<int,int>`）：
  ```cpp
  using Pos = std::pair<int,int>;
  // 返回含起点、终点的完整路径；无解返回空 vector
  std::vector<Pos> bfsFind(const std::vector<std::vector<char>>& grid,
                           Pos start, Pos goal);
  std::vector<Pos> dijkstraFind(const std::vector<std::vector<char>>& grid,
                                Pos start, Pos goal);
  ```

- **编码步骤**：
  - [ ] 抽取私有辅助函数：判断越界 `inBounds`、判断通行 `isOpen(grid,r,c)`（`grid[r][c]==' '` 才可走、`'#'` 跳过），两个算法共用，避免重复代码。
  - [ ] **BFS**：`queue` 起点入队；分层扩展四方向；发现 `goal` 即停止；用 `parent[][k]` 从终点向前追溯回起点，再逆序得到正序路径。
  - [ ] **Dijkstra**：`dist` 初始化为大数，优先队列按 `(dist, Pos)` 存储，弹出当前代价最小的节点，对其邻居做「松弛」`if (nd < dist[nr][nc])` 才更新并入队。
  - [ ] 自测：用一个小固定迷宫（手工构造）分别跑 BFS 与 Dijkstra，确认两者路径长度一致、且起点终点都在路径内。

---

#### 成员A：核心工具（随机数 / 计时 / 打印美化）

- **负责文件**：`include/core/Utils.h`、`src/core/Utils.cpp`

- **选用方法**：全部只用 C++17 标准库，不引第三方。

- 随机数工具：
  - 暴露一个全局随机源 `std::mt19937& rng()`，内部用 `std::random_device` 做种子（默认每次不同）；再提供一个 `setSeed(unsigned)`，便于复现同一迷宫。
  - [ ] 验证：同一种子调用生成器两次，迷宫应完全相同。

- 计时器（RAII 风格）：
  ```cpp
  class ScopedTimer {
      std::chrono::steady_clock::time_point _t0;
  public:
      ScopedTimer();
      ~ScopedTimer();   // 析构时打印 "finished in xx ms"
  };
  ```
  - [ ] 在 main 里包裹生成、求解，输出耗时。

- 打印美化：
  ```cpp
  // path 为空则只打印迷宫；非空则用 '.' 高亮路径
  void printMaze(const std::vector<std::vector<char>>& grid,
                 const std::vector<Pos>& path,
                 const Pos& start, const Pos& goal);
  ```
  - 约定字符：`#` 墙、` ` 路、`S` 起点、`E` 终点、`.` 求解走过的路径（路径中非起点终点的格子覆盖为 `.`，起点终点仍优先显示 `S`/`E`）。
  - [ ] 先用手工小迷宫验证排版对齐，再接入生成器输出。

---

#### 双方协作：main.cpp 串联流程

两人先在文档里对齐下面是「数据 & 接口契约」，再由任一人实现：

```cpp
int main(int argc, char** argv) {
    unsigned W = 21, H = 21;              // 默认 21×21（奇数）
    // 可选：从命令行解析尺寸、种子
    DfsGenerator gen(W, H);
    gen.generate();
    auto grid = gen.grid();
    auto S = gen.start(), G = gen.goal();

    printMaze(grid, {}, S, G);                            // 1) 打印空迷宫
    { ScopedTimer t; auto p = bfsFind(grid, S, G);
      printMaze(grid, p, S, G); }                         // 2) BFS 路径
    { ScopedTimer t; auto d = dijkstraFind(grid, S, G); } // 3) Dijkstra 对拍
}
```

**本周验收清单**
- [ ] `build.bat` 编译 0 错误；运行 `./maze_app` 正常打印迷宫。
- [ ] 可改尺寸（如 21、41、101）且迷宫始终连通、起点可到终点。
- [ ] BFS 与 Dijkstra 输出的路径长度一致。
- [ ] 生成与求解耗时能通过 ScopedTimer 打印出来。
- [ ] 双方各自代码先单独能编译，再合入 main 联调，避免大段冲突。

### 第 3 周：可视化（重点）

> 本周目标：让迷宫和路径真正「显示在窗口里」。复用第 2 周跑通的 `DfsGenerator` + `BFS/Dijkstra` 结果，接入图形库把它们画出来，并在屏幕上高亮一条求解路径。这是整个项目最亮眼的一周，也最容易出现环境坑，因此结尾给出「终端可视化兜底」方案。

**影片产出 / 验收**：运行程序会弹出一个图形窗口，显示迷宫；按某键或用接口触发求解后，窗口里用高亮色从起点到终点画出路径动画。

---

#### 成员A：接入图形库并渲染迷宫

- **负责文件**：`include/visual/Renderer.h`、`src/visual/Renderer.cpp`（第 2 周的分工里可视化归成员A）

- **选用方法**：
  - 图形库沿用第 1 周决策：首选 **SFML**，备选 **raylib**。下面以 SFML 为例，raylib 思路相同。
  - 统一把第 2 周的 `Grid`（`vector<vector<char>>`）作为渲染输入，不直接读 `Maze` 私有格，保证成员A/B 的解耦。
  - **渲染模型**：把每个格子画成一个矩形。格子尺寸 `cellSize`（如 20 像素），窗口大小 = `height*cellSize × width*cellSize`。
  - **坐标映射**：迷宫逻辑坐标 `(r,c)` → 屏幕像素 `(c*cellSize, r*cellSize)`。

- **推荐接口**：
  ```cpp
  class Renderer {
  public:
      Renderer(int cellSize = 20);
      // 画一整张迷宫（墙用深色、路用浅色，起点/终点用醒目标记）
      void draw(const std::vector<std::vector<char>>& grid,
                const Pos& start, const Pos& goal);
      // 在窗口上叠加一条路径并逐帧高亮（动画）
      void drawPath(const std::vector<Pos>& path);
      // 打开/关闭窗口主循环（含事件处理、逐帧刷新）
      int run();   // 返回退出码
  };
  ```

- **编码步骤**：
  - [ ] 建窗口：`sf::RenderWindow window({w,h}, "Maze")`。
  - [ ] 写「渲染一帧」函数：遍历 `grid` 的每格，按 `'#'`/`' '`/`'S'`/`'E'` 选择颜色 `sf::RectangleShape` 并 `window.draw`。
  - [ ] 事件循环：用 `while (window.isOpen())` 轮询事件，`sf::Keyboard::isKeyPressed` 监听空格 / 回车来触发求解。
  - [ ] 实现路径动画：把 `drawPath` 的路径切成若干段，每帧多画几个格子，逐帧累积 → 达到「从起点生长到终点」的效果。
  - [ ] **兜底方案（重要）**：若图形库始终装不上，本周也可先用「延时打印」实现终端动画——每 50ms 往 `printMaze` 里多写几个 `.`，配合 `std::this_thread::sleep_for` 清屏重绘，同样能演示路径生长。第 3 周里程碑以此兜底版本也能验收。

---

#### 成员B：A* 算法 + 求解器单测

- **负责文件**：`include/solver/AStar.h`、`src/solver/AStar.cpp`、`tests/test_solver.cpp`

- **选用方法**：
  - **A\***：用优先队列（最小堆）+ 启发式函数。迷宫里用**曼哈顿距离** `|r1-r2| + |c1-c2|` 作估价函数（保证一致可行），配合 `g(n)`（实际代价）与 `f(n)=g(n)+h(n)` 选路。这比 BFS 更快地朝终点收拢。
  - **GoogleTest**：为求解器（BFS / Dijkstra / A*）各写一个可重复的小用例断言路径正确，防止后续改动回归。

- **推荐接口**：
  ```cpp
  // 与第2周求解器签名保持一致
  std::vector<Pos> aStarFind(const std::vector<std::vector<char>>& grid,
                             Pos start, Pos goal);
  ```

- **编码步骤**：
  - [ ] 实现 `aStarFind`：优先队列按 `f` 排序，`gScore` 数组记录从起点到当前格的最小代价，每当找到更小的 `g` 就入队；记录 `cameFrom` 用于回溯路径。
  - [ ] 启发式用曼哈顿距离；在优先队列里若 `f` 相同用行号打破平局，保证结果稳定。
  - [ ] 用三个小迷宫（直线 / 折线 / 带死胡同）分别断言 A\*、BFS、Dijkstra 三条路径长度一致。
  - [ ] 接入 GoogleTest：写 `test_solver.cpp`，用 `EXPECT_EQ(path.front(), start)`、`EXPECT_EQ(path.back(), goal)` 等断言；在 `build.bat` 里加一条独立编译命令产出 `test_solver.exe`（不并入 `maze_app.exe`）。若装 GoogleTest 麻烦，可先用「断言 + main 里手动检查」替代（不强制引第三方）。

---

#### 双方协作：集成联调

- **方法**：成员A 不假设成员B 的具体实现，只依赖第 1 周定下的「契约」接口；两人分别在自己的分支上开发，最后合并到 `main`。

- **编码步骤**：
  - [ ] 成员B 把 A\* 加入 `Solver.h`（或新增 `AStar.h`），通知成员A 新增的函数签名。
  - [ ] 成员A 在 `main.cpp` 里把图形窗主循环与「生成→求解」链接起来：生成迷宫 → 渲染 → 按键触发 `aStarFind` → `drawPath` 动画。
  - [ ] 双方在 `main` 分支做一次集成构建，用 git 合并解决两人文件冲突（大概率冲突只在 `main.cpp` 与 `build.bat`，尽量各自少动它们）。
  - [ ] 跑一遍 `build.bat` + 运行，确认窗口能显示迷宫并能呈现高亮路径。

**本周验收清单**
- [ ] 图形窗口能正确显示迷宫（墙/路/起点/终点颜色分明）。
- [ ] 触发求解后，窗口内从起点到终点逐渐高亮出一条路径。
- [ ] A\*、BFS、Dijkstra 三条路径长度一致（或有单测断言佐证）。
- [ ] GoogleTest 用例能通过直接运行 `test_solver.exe`（或用手写断言自检）。
- [ ] 若图形库不可用，用「终端动画兜底」也能演示路径生长，仍算通过。

### 第 4 周：算法效率优化

> 本周目标：让程序能平滑处理 101×101 以上的大迷宫——生成不卡顿、求解快速。同时把迷宫落地成**可保存/加载的文件**，并支持命令行参数指定尺寸。产出为「性能报告 + 更快更大」的可运行版本，为后面几周打基础。

**影片产出 / 验收**：能用命令行生成 101×101 迷宫并求解出路径，耗时以计时器打印；能把迷宫存成文件再读回来，结果一致。

---

#### 成员B：生成器与求解器性能优化

- **负责文件**：`src/generator/DfsGenerator.cpp`、`src/solver/{Solver,AStar}.cpp`

- **选用方法**：
  - 先**测再优**：用第 2 周的 `ScopedTimer` 对 21/41/101 尺寸分别测生成与求解耗时，找到瓶颈再动手，不要盲目改。
  - **生成器优化**：递归回溯在大迷宫时递归深、抖动大。可选改进：
    - 用**迭代版 DFS**（显式 `std::stack`）代替递归，消除爆栈风险。
    - 只对「奇数」格子做处理、预先分配内存，减少 `vector` 反复 resize。
  - **求解器优化**：
    - A* 的优先队列用 `std::priority_queue` 已够用，重点优化**启发式**：确保用曼哈顿距离（可采纳、一致），避免走冤枉路。
    - 若实测 101×101 已够快（< 几百 ms），则不过度优化，把时间留给正确性验证。

- **编码步骤**：
  - [ ] 建立统一性能测量程序 `tools/benchmark.cpp`：循环若干次，输出平均生成/求解耗时（ms），按尺寸分档。
  - [ ] 若递归 DFS 在 201×201 崩栈，则改写为迭代栈版本。
  - [ ] 对有环或无环迷宫各测一遍，记录 A* 与 BFS 在统计平均节点访问次数（体现启发式剪枝效果）。
  - [ ] 输出 `docs/perf_report.md`：记录尺寸、算法、耗时、内存，作为第 4 周里程碑。

---

#### 成员A：文件保存/加载 + 可配置参数

- **负责文件**：`include/core/MazeIO.h`、`src/core/MazeIO.cpp`、`src/main.cpp`

- **选用方法**：定义一种**简单文本格式**，规则自洽、人可读、易调试。首行写尺寸，后面逐行写迷宫字符。

- **建议的迷宫文件格式**（约定写入 `docs/interface.md`）：
  ```
  21 21
  #################
  # #     #  .... #
  ...（后续每一行是一个字符串：'#' 墙 / ' ' 路 / 'S' / 'E'）
  ```

- **推荐接口**：
  ```cpp
  // MazeIO.h
  bool saveMaze(const std::vector<std::vector<char>>& grid,
                const std::string& filename);   // 成功返回 true
  bool loadMaze(const std::string& filename,
                std::vector<std::vector<char>>& out);  // 读回 out
  ```

- **编码步骤**：
  - [ ] `saveMaze`：首行写 `h w`，接 `h` 行字符串；用文件输出流 `std::ofstream`。
  - [ ] `loadMaze`：首行读尺寸，逐行读字符填格子；用 `std::ifstream`；对行数/列数不符时返回 `false` 并打印错误。
  - [ ] **命令行参数**：在 `main` 里用 `argc/argv` 解析，支持 `--size 101`、`--seed 123`、`--load file.maze`、`--save file.maze`、`--save` 无参时退出。
  - [ ] 做一个自测：存一个迷宫 → 读回 → `printMaze` 对比是否一致（或构建一个函数 `isEqual` 比对）。

---

#### 双方协作：大迷宫实测

- **方法**：以 `tools/benchmark.cpp` 为共同脚本，两人各自跑分汇总。

- **编码步骤**：
  - [ ] 尺寸分档：`21`、`41`、`101`，可选加 `201`。
  - [ ] 记录生成耗时（迭代 vs 递归）、求解耗时（BFS vs A*）。
  - [ ] 数据汇总到 `docs/perf_report.md`，签名确认人各自注明。

**本周验收清单**
- [ ] `./maze_app --size 101 --seed 42` 能生成并求解，耗时打印正常。
- [ ] `--save` 存出的文件再用 `--load` 读回后迷宫一致。
- [ ] `benchmark.cpp` 能跑出三档尺寸耗时，报告已生成（`docs/perf_report.md`）。
- [ ] 101×101 生成 + 求解总耗时明显可接受（经验参考：< 数百 ms）。

### 第 5 周：扩展功能①多终点

> 本周目标：从「一个起点到一个终点」升级为「一个起点、多个终点，找出一条**能串起所有终点的最短路径**」。典型应用：快递员从仓库出发送完所有收货点选最短路线（对应旅行商简化版）。重点在**算法正确性**，可视化是加分项。

**影片产出 / 验收**：给定起点和若干个终点，程序能算出一条连续路径，按顺序经过所有终点并回到起点（要求覆盖所有终点），图形界面上能看到这条「串起来」的路径。

---

#### 成员B：多终点最短路径算法

- **负责文件**：`include/solver/MultiGoal.h`、`src/solver/MultiGoal.cpp`

- **选用方法**（两种思路，先用简单稳定的）：
  1. **多次 Dijkstra / BFS + 最短哈密顿近似**（推荐先做）：
     - 先把所有「关键点」：起点 S + 全部终点 → 任意两点间在迷宫里的最短距离先用 BFS/Dijkstra 各算一遍。
     - 得到一个**带权完全图**（节点=关键点，边权=迷宫内最短距离）。
     - 在这个小图上求「从 S 出发、覆盖所有终点并回起点」的顺序。因为关键点通常很少（如 ≤ 8），可以用**枚举排列 / 位压缩 DP（状态压缩）**求精确最小路线。
  2. **多源 BFS**：从多个终点同时反向 BFS，记录每个格子的「到达哪些终点」——适合「求每个格到最近终点」的场景，但不直接给出串行路径，仅作辅助。

- **推荐接口**：
  ```cpp
  // 输入：迷宫、起点、终点列表
  // 输出：一条从起点出发按序经过所有终点的路径（若无法全部到达返回空）
  std::vector<Pos> multiGoalFind(const std::vector<std::vector<char>>& grid,
                                 Pos start,
                                 const std::vector<Pos>& goals);
  ```

- **编码步骤**：
  - [ ] 对关键点集合（S + goals）两两调用第 2 周的 `bfsFind`/`dijkstraFind`，构建距离矩阵 `dist[i][j]`。
  - [ ] 实现「找访问顺序」：终点数 ≤ 8 用**状态压缩 DP**（`dp[mask][last]`，mask 表示已访问终点集合），返回顺序 + 总长度。
  - [ ] 按顺序拼接各段子路径（去掉重复端点），组成一条连续路径，填入 `multiGoalFind` 返回。
  - [ ] 自测：构造 2~3 个终点的小迷宫，验证路径确实经过全部终点、且总长度最短。

---

#### 成员A：界面多终点交互与高亮

- **负责文件**：`include/visual/Renderer.h`、`src/visual/Renderer.cpp`、`src/main.cpp`

- **选用方法**：在图形界面上支持**放置多个终点**（如点击空白格切换为终点标记 `E`），物理存储改为 `vector<Pos> goals`，传给 `multiGoalFind` 后把整条路径高亮。

- **编码步骤**：
  - [ ] 渲染时把多个终点各画成醒目标记（如不同颜色 `E`），而不是只认单个终点。
  - [ ] 用鼠标事件 `sf::Mouse` 监听左键点击：把点击格设为终点（`'E'`），再次点同一格则移除。
  - [ ] `main` 里收集 `goals` 列表，触发 `multiGoalFind`，把返回的整条路径交给 `drawPath` 逐段高亮。
  - [ ] 若图形库不可用，终端版本：`printMaze` 支持多个终点标记，打印路径用 `.` 覆盖经过的所有终点。

---

#### 双方协作：集成与边界

- **编码步骤**：
  - [ ] 成员B 明确 `multiGoalFind` 的输入输出（已定契约），成员A 按它对接。
  - [ ] 处理边界：个别终点不可达（被墙包围）时，统一约定返回空路径并在界面/终端提示「某终点不可达」。
  - [ ] 联调：界面上放 3 个终点，验证路径确实依次经过它们。

**本周验收清单**
- [ ] 给定 2~4 个终点时能算出并画出覆盖全部终点的路径。
- [ ] 存在不可达终点时程序不崩溃，能给出明确提示。
- [ ] 路径总长度符合「经过所有终点的最短」近似/精确要求。
- [ ] 图形（或终端）都能展示多终点路径。

### 第 6 周：扩展功能②动态障碍物与重规划

> 本周目标：让迷宫在运行中能**变化**——某些墙突然变成路、或某些路变成墙——而程序能**自动重算路径**并实时更新，模拟机器人重规划（机器人、导航常见需求）。重点是「变化触发重算」的机制与稳定性。

**影片产出 / 验收**：运行中用户改变某个格子（路⇄墙），界面上的路径自动更新；若路径被阻断，能看到绕行后的新路径或合理的「暂不可达」提示。

---

#### 成员B：动态障碍物模块 + 重规划机制

- **负责文件**：`include/solver/Repath.h`、`src/solver/Repath.cpp`

- **选用方法**（两个难度档，先用简单可靠方案）：
  - **方案A（推荐先做）：定期 / 事件触发式重算**——维护当前 `grid`；当任何一格状态变化时，直接**重新调用 A\***（或 BFS）跑一遍得到新路径。实现简单、结果正确，代价是对大迷宫频繁重算较慢；对 21×21、41×41 完全够用。
  - **方案B：增量式（D\* Lite 思路）**——只更新受影响区域的开销，适合 101×101 以上且变化频繁。较难，作为本周加分项；标题先不直接上 D*，避免难度过高。

- **推荐接口**：
  ```cpp
  // 场景管理：记录当前迷宫与现状路径，状态变化时重算
  class RepathPlanner {
  public:
      // 用初始迷宫、起点、终点初始化，算出初始路径
      RepathPlanner(const std::vector<std::vector<char>>& grid, Pos s, Pos g);
      // 把 (r,c) 改成 ch 并触发重算（ch 为 ' ' 或 '#' 等）
      void setCell(int r, int c, char ch);
      bool hasPath() const;                 // 当前是否有可行路径
      const std::vector<Pos>& path() const; // 最新路径
  };
  ```

- **编码步骤**：
  - [ ] 类内持有 `_grid`（可变副本）与 `_path`。
  - [ ] `setCell`：更新 `_grid`；若改动位置在原路径上（或使用者主动要求），调用内部 `_recompute()` 用 A\* 重算 `_path`。
  - [ ] `setCell` 里用 `hasPath` 区分「可到达」与「被阻断」，阻断时 `_path` 清空。
  - [ ] 自测：一条直线路径中间被放墙 → 断言新路径绕行且有效、或报告不可达。
  - [ ] （加分）实现小规模增量启发式优化，避免整图重扫。

---

#### 成员A：界面模拟障碍物变化并实时展示

- **负责文件**：`include/visual/Renderer.h`、`src/visual/Renderer.cpp`、`src/main.cpp`

- **选用方法**：在图形窗口里允许用户「切换格子」（点到墙↔路），每切换一次就调用 `RepathPlanner::setCell` 拿新路径重画，形成「路径实时更新」的直观演示。

- **编码步骤**：
  - [ ] 鼠标右键（或某按键）点击某格：在 `' '` 与 `'#'` 之间切换（墙⇄路）。
  - [ ] 每次格子变化后调用 `planner.setCell(r,c,ch)`，读取最新 `path()` 重画高亮。
  - [ ] 动画：路径重画时用「淡入/刷新」表现实时更新；若 `hasPath()==false`，界面用提示文字「当前无路径」。
  - [ ] 终端兜底：用键盘输入「坐标+类型」改变格子，`printMaze` 每轮刷新打印新路径。

---

#### 双方协作
- **编码步骤**：
  - [ ] 成员B 提供 `RepathPlanner` 契约，成员A 的窗口事件对接 `setCell`/`path()`。
  - [ ] 边界约定：起点或终点被改成墙时，明确提示「终点被堵，请先恢复」，程序不崩溃。
  - [ ] 联调演示：人为堵住当前路径中央若干格，观察路径自动绕开。

**本周验收清单**
- [ ] 运行中能改变格子，路径自动更新为合理的新路径。
- [ ] 路径被彻底阻断时能给出「不可达」提示而不崩溃。
- [ ] 恢复受阻格子后路径能自动恢复最短路。
- [ ] （加分）对大迷宫实现增量式重规划，重算耗时明显下降。

### 第 7 周：自定义编辑 + 整合收尾

> 本周目标：把前面的功能收拢成**一个完整、好用、可交付**的成品。让用户能**自定义迷宫**（自由画墙、设起点终点），同时提供一个清晰的入口（命令行/界面）来使用所有能力，最后统一查 bug、补文档、录演示。

**影片产出 / 验收**：一个打包好、可执行、可演示的程序；用户能自己画迷宫并即时求解；配套 `README` 使用说明与一段演示视频。

---

#### 成员A：手动创建 / 编辑迷宫（交互编辑）

- **负责文件**：`include/visual/Editor.h`、`src/visual/Editor.cpp`、`src/main.cpp`

- **选用方法**：给图形界面加一个「编辑模式」，支持鼠标操作画墙、删墙、设起点/终点。

- **交互约定**（可做成工具栏按钮或快捷键）：
  - 左键拖拽：画墙（把空格/路设为 `'#'`）。
  - 右键拖拽（或按住某键）：删墙（设为路 `' '`）。
  - 单独快捷键：`S` 把当前格设为起点；`E` 设为终点；多个终点沿用第 5 周逻辑。
  - 点「求解」触发 `aStarFind` / `bfsFind`，实时高亮。

- **编码步骤**：
  - [ ] 以 `Renderer` 为底，扩展出编辑交互逻辑（hit-test 网格：把像素坐标÷cellSize 得到格子坐标）。
  - [ ] 维护一个当前「编辑工具」（画墙/删墙/设起点/设终点），用鼠标/键盘事件切换。
  - [ ] 拖拽实现：鼠标按下到移动都持续修改格子（用 `sf::Mouse::isButtonPressed` 在 `while` 循环取样）。
  - [ ] 编辑时同样把 `grid` 保存在 `RepathPlanner`（第 6 周）里，实现「画完即求解」。
  - [ ] `main` 提供「编辑模式」与「演示模式」两个入口（命令行参数切换）。

---

#### 成员B：尺寸 / 复杂度预设 + 入口整理

- **负责文件**：`include/generator/Generator.h`（或新增 `Presets.h`）、`src/main.cpp`

- **选用方法**：把常用配置做成「预设」，并提供一个清晰的命令行/界面选单，方便演示。

- **预设建议**：
  | 预设 | 尺寸 | 说明 |
  |------|:----:|------|
  | `tiny`  | 11×11 | 快速演示 |
  | `small` | 21×21 | 默认 |
  | `medium`| 41×41 | 常规 |
  | `large` | 101×101 | 性能展示 |
  | `complex` | 与尺寸相关 + 更高连通度 | 体现复杂度调节 |

- **编码步骤**：
  - [ ] 定义预设表 `struct Preset { std::string name; unsigned w, h; };` 并提供 `lookup(name)`。
  - [ ] 命令行统一解析：`--preset small` / `--size 31 31` / `--mode edit|play` 等（与第 4 周参数合并）。
  - [ ] 界面入口：提供一个小菜单（或快捷键显示操作说明），把「生成 / 编辑 / 多终点 / 动态障碍」串进主循环。
  - [ ] 整理「复杂度」：生成器可通过「额外打通随机墙」控制连通度，接口 `generate(complexity)`。

---

#### 双方协作：整体联调、修复、文档与演示

- **编码步骤**：
  - [ ] 统一走一遍「编辑迷宫 → 求解 → 切换障碍 → 多终点」的完整用户流程，逐个修 Bug。
  - [ ] 确认两人环境都能 `build.bat` 编译并运行，无平台差异问题。
  - [ ] 补 `README.md`：依赖安装、编译步骤、运行示例、各快捷键/命令行参数说明。
  - [ ] 录制一段演示（屏幕录制：生成 → 求解 → 画墙重算 → 多终点），整理为交付物。
  - [ ] 用 `git tag` 打一个 `v1.0` 发布版本，并把完整项目归档。

**本周验收清单**
- [ ] 能手动画迷宫、设起点/终点、即时求解并高亮。
- [ ] 支持 `tiny~large` / `complex` 预设，命令行与界面入口清晰。
- [ ] 两个成员各自 clone 后都能一键编译、运行并复现演示流程。
- [ ] README 与实际行为一致，演示视频已录好。
- [ ] 已发布 `v1.0` 并归档源码。

---

## 五、任务均衡核对

> 下面用"完整模块"为单位粗估工作量，确保两边大致均衡。

| 成员A | 成员B |
|------|------|
| 数据模型 Maze 类 | 生成器算法 |
| 可视化渲染 | 求解算法（BFS/A*/Dijkstra）|
| 核心工具（计时/随机/序列化）| 算法优化与单测 |
| 手动编辑交互 | 多终点与动态障碍算法 |
| 文件 IO、配置 | 复杂度/尺寸预设 |

两人都在各自线上有独立的编码任务，只在可视化、接口设计、最终联调三处需要协同，尽量避免任务重叠造成冲突。

---

## 六、建议的协同工作流程

1. **每日/隔日同步**：同步进度、卡点，避免接口不一致。
2. **接口先行**：先约定好模块之间的头文件接口（如 `Maze`、`Path`），再各自实现，可减少集成冲突。
3. **分支开发**：每人一个功能分支，完成后再合并到 `main`，合并前跑通测试。
4. **定期里程碑**：以每周结束的目标为准，提前暴露风险。

---

## 七、风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 图形库环境安装困难 | 可视化进度受阻 | 先做终端字符可视化兜底，保证核心可演示 |
| 大迷宫性能差 | 体验下降 | 第4周专项优化，必要时用启发式+剪枝 |
| 双人接口冲突 | 集成耗时 | 提前定接口、频繁同步、功能分支 |
| 动态障碍物算法过难 | 阻塞 | 可用"定期重新 A*"的简化方案替代 D*，先保证功能 |

---

## 八、交付物清单

- 完整可编译运行的 C++ 工程源码
- 随机迷宫生成（多种算法可选）
- 至少 3 种求解算法（BFS / Dijkstra / A*）
- 支持图形化可视化与终端两种模式
- 多终点最短路径
- 动态障碍物实时重规划
- 手动编辑迷宫（自定义起点/终点/障碍物）
- 使用说明文档与演示
```