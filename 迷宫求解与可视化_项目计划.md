# 迷宫求解与可视化 — 两人项目计划

## 一、项目简介

- **选题**：迷宫生成器 + 求解器
- **语言**：C++17
- **构建**：g++ 命令行 + 一键脚本（`build.bat` / `build.sh`），**不使用 CMake**
- **周期**：7 周
- **团队**：2 人（成员A、成员B）

**功能清单**
| 模块 | 说明 | 难度 |
|------|------|:----:|
| 迷宫生成器 | 随机生成二维矩阵（墙/通路），多种算法 | 中 |
| 路径求解器 | 找到起点到终点的有效路径（BFS / A* / Dijkstra） | 中 |
| 路径可视化 | 终端字符渲染迷宫与路径（`#`/空格/`S`/`E`/`.`，带动画） | 基础 |
| 算法优化 | 大迷宫性能优化（递归→迭代、启发式调优、内存预分配） | 挑战 |
| 多终点 | 计算到达所有终点的最短路径 | 挑战 |
| 动态障碍物 | 实时变化环境下的路径重规划 | 挑战 |
| 自定义编辑 | 手动创建迷宫、自定义起点/终点/障碍物 | 扩展 |

---

## 二、目录结构（简洁）

```
MazeProject/
├── build.bat              # Windows 一键编译（build.sh 供 Mac/Linux）
├── include/               # 头文件，按模块分包，与 src 一一对应
│   ├── core/              # 通用：Common(契约)、Utils、MazeIO
│   ├── model/             # Maze 数据模型
│   ├── generator/         # 生成算法
│   └── solver/            # 求解算法
├── src/                   # 实现（与 include 同结构）
│   ├── core/  model/  generator/  solver/
├── tests/                 # 单元测试 / 自测
├── docs/                  # 接口契约、性能报告、说明文档
└── README.md
```

- **分支策略**：`main`（稳定主干）+ 每人一个功能分支（`member-a` / `member-b`）
- **命名约定**：头文件 `.h`、实现 `.cpp`；组件名首字母大写（`Maze`、`DfsGenerator`、`Solver`）

---

## 三、分工

| 成员 | 主要模块 |
|:----:|----------|
| 成员A | 数据模型 Maze、终端渲染 printMaze、核心工具（随机/计时/文件IO）、键盘编辑交互 |
| 成员B | 生成器、求解器（BFS/A*/Dijkstra）、多终点与动态障碍算法、单测优化 |

> 两人各有一条独立开发线，仅在**接口契约、可视化集成、最终联调**三处协同，避免冲突。

---

## 四、每周计划（详细）

> 通用约定（全程有效）：字符约定 `'#'`墙 / `' '`路 / `'S'`起点 / `'E'`终点；坐标 `Pos=(row,col)`；契约见 `docs/interface.md`。每个人在自己的分支（`member-a`/`member-b`）开发，完成后再合并到 `main`。

---

### 第 1 周：骨架 + 契约

**本周目标**：两台机器 clone 后都能一键编译，终端打印一个固定小迷宫。

#### 双方协作：搭建骨架
- [ ] 建目录（见"二、目录结构"），写临时 `main.cpp` 打印 `"hello maze"`。
- [ ] 写 `build.bat`：
  ```bat
  @echo off
  g++ -std=c++17 -I include -o maze_app.exe src/main.cpp src\model\Maze.cpp
  if errorlevel 1 (echo 编译失败 & exit /b 1)
  ```
  > Mac/Linux 用同内容 `build.sh`（`\` 换 `/`）。后续加了新 `.cpp` 就在命令里追加。
- [ ] `.gitignore` 含 `*.exe`、`*.out`；`git init` 提交并推送。
- [ ] 双方各 clone 一遍跑通 `build.bat`，确认环境一致。
- [ ] 确定采用**终端字符方案**：不使用任何图形库，全程命令行/键盘交互（环境零依赖，两台机器行为一致）。

#### 成员A：Maze 数据模型
- **负责文件**：`include/model/Maze.h`、`src/model/Maze.cpp`
- **方法**：面向对象封装，内部 `vector<vector<char>>`，对外只暴露安全接口。
- **接口**：`Maze(h,w)`、`height()/width()`、`inBounds(r,c)`、`isWall(r,c)`、`setCell(r,c,ch)`、`cell(r,c)`、`print()`。
- **步骤**：构造全填 `'#'` → 实现判断/设置（越界安全返回）→ `print()` 按行拼接整齐输出 → 写 9×9 手工迷宫在 `main` 验证打印。

#### 成员B：接口契约
- **负责文件**：`docs/interface.md`（本周只写文档）
- **方法**：先"纸上定契约"，避免两周后两人结构不兼容。
- **步骤**：对比二维数组/邻接表/四叉树优劣，选定**二维 `char` 矩阵** → 定 `using Pos = std::pair<int,int>`、`using Grid = std::vector<std::vector<char>>` → 定生成器/求解器函数签名 → 给成员A确认后定稿。

**本周验收清单**
- [ ] 全新环境 `build.bat` 编译成功并打印 9×9 迷宫。
- [ ] `docs/interface.md` 已定稿且两人确认。
- [ ] Git 仓库已建、初始提交已推送。

---

### 第 2 周：生成器 + 求解器基础

**本周目标**：终端跑通「生成 → 求解 → 打印路径」完整闭环（不依赖图形库）。

#### 成员B：DFS 生成器
- **负责文件**：`include/generator/DfsGenerator.h`、`src/generator/DfsGenerator.cpp`
- **方法**：递归回溯。从起点挖路、隔一跳拆墙，保证是**完美迷宫**（唯一通路、整体连通）。
- **接口**：
  ```cpp
  class DfsGenerator {
  public:
      DfsGenerator(unsigned w, unsigned h);          // w/h 为奇数
      void generate();
      const Grid& grid() const;
      Pos start() const;   // (1,1)
      Pos goal()  const;   // (h-2, w-2)
  };
  ```
- **步骤**：构造全填 `'#'` → `carve(r,c)`：当前格挖成路，`std::shuffle` 打乱四方向，对隔一跳的墙格先打通中间墙再递归 → 起点终点固定 `(1,1)`/`(h-2,w-2)` → 打印验证通路。

#### 成员B：BFS / Dijkstra 求解器
- **负责文件**：`include/solver/Solver.h`、`src/solver/Solver.cpp`
- **方法**：BFS 用 `std::queue` 求无权最短路；Dijkstra 用 `std::priority_queue`（最小堆）+ 松弛，为第 6 周加权场景打基础。
- **接口**：`std::vector<Pos> bfsFind(const Grid&, Pos, Pos)`、`dijkstraFind(...)`；无解返回空 `vector`。
- **步骤**：抽公共辅助 `inBounds/isOpen`（`'#'` 不可走）→ BFS：队列+`visited`+`parent` 回溯 → Dijkstra：`dist` 大数初始化+松弛+过期节点跳过 → 手写 `tests/test_solver.cpp` 断言：直线/折线/无解三用例、两算法路径长度一致。

#### 成员A：核心工具
- **负责文件**：`include/core/Utils.h`、`src/core/Utils.cpp`
- **方法**：只用 C++17 标准库。
- **步骤**：全局随机源 `std::mt19937& rng()` + `setSeed(unsigned)`（同种子可复现）→ RAII 计时器 `ScopedTimer`（析构打印耗时 ms）→ `printMaze(grid, path, S, G)`（路径用 `.` 高亮，起点终点仍显 `S`/`E`）→ 在 `main` 串联生成+求解+打印。

**本周验收清单**
- [ ] `build.bat` 编译 0 错误，`maze_app` 正常打印迷宫与路径。
- [ ] BFS 与 Dijkstra 路径长度一致；无解返回空。
- [ ] `test_solver.exe` 全部用例通过。

---

### 第 3 周：终端可视化 + A*

**本周目标**：在终端清晰、带动画地展示迷宫和求解路径（不依赖任何图形库）。

#### 成员A：终端渲染 + 路径动画
- **负责文件**：`src/core/Utils.cpp`（增强 `printMaze`）、`src/main.cpp`
- **方法**：纯终端字符渲染，全平台一致。清屏用 `system("cls")`（Windows）/ `system("clear")`（Mac/Linux）。
- **步骤**：增强 `printMaze`（行列号、分隔线，起点终点醒目标记）→ 路径生长动画：每 50ms 往路径里多画几个 `.` 并清屏重绘 → 主循环监听按键（空格/回车触发求解、`Q` 退出）→ 求解前后分别打印空迷宫与带路径迷宫。

#### 成员B：A* 算法 + 单测
- **负责文件**：`include/solver/AStar.h`、`src/solver/AStar.cpp`、`tests/test_solver.cpp`
- **方法**：优先队列最小堆 + 曼哈顿启发式 `f = g + h`，比 BFS 更快朝终点收拢。
- **步骤**：实现 `aStarFind`（`gScore` 数组 + `cameFrom` 回溯）→ 启发式用曼哈顿距离，`f` 相同时用行号打破平局 → 补 3 个迷宫用例断言 A*/BFS/Dijkstra 长度一致 → 编译方式在 `build.bat` 追加一行产出 `test_solver.exe`（GoogleTest 麻烦时可用 `assert` 替代）。

**本周验收清单**
- [ ] 终端清晰显示迷宫（墙/路/起点/终点分界明显）。
- [ ] 触发求解后终端从起点到终点逐渐点亮路径（动画）。
- [ ] A*/BFS/Dijkstra 路径长度一致（单测佐证）。
- [ ] 大尺寸迷宫（41×41）动画仍可流畅播放。

---

### 第 4 周：算法效率优化

**本周目标**：平滑处理 101×101 大迷宫，支持保存/加载文件与命令行参数。

#### 成员B：性能优化
- **负责文件**：`src/generator/DfsGenerator.cpp`、`src/solver/{Solver,AStar}.cpp`
- **方法**：先测再优。用 `ScopedTimer` 对 21/41/101 尺寸测生成与求解耗时，找到瓶颈再动手。
- **步骤**：建 `tools/benchmark.cpp`（循环多次输出平均耗时，按尺寸分档）→ 若递归 DFS 在 201×201 崩栈，改写为**迭代栈**（显式 `std::stack`）→ 确认 A* 启发式可采纳、避免绕路 → 大迷宫先预分配 `vector` 内存 → 输出 `docs/perf_report.md`。

#### 成员A：文件 IO + 命令行
- **负责文件**：`include/core/MazeIO.h`、`src/core/MazeIO.cpp`、`src/main.cpp`
- **方法**：简单文本格式，人可读易调试。首行写尺寸，之后逐行写迷宫字符。
- **步骤**：`saveMaze(grid,file)`（`std::ofstream` 写 `h w`+逐行）→ `loadMaze(file,out)`（`std::ifstream` 读回，行列不符返回 false）→ `main` 解析 `argc/argv`：`--size 101`、`--seed 123`、`--load f.maze`、`--save f.maze` → 自测存→读→比对一致。

**本周验收清单**
- [ ] `maze_app --size 101 --seed 42` 生成+求解流畅（< 数百 ms）。
- [ ] `--save` 后再 `--load` 迷宫一致。
- [ ] `benchmark` 跑出三档耗时，报告已生成。

---

### 第 5 周：多终点

**本周目标**：一个起点、多个终点，找出串起所有终点的最短路径。

#### 成员B：多终点算法
- **负责文件**：`include/solver/MultiGoal.h`、`src/solver/MultiGoal.cpp`
- **方法**：关键点（S+全部终点）两两求迷宫最短路建距离矩阵 → 在关键点构成的小图上用**状态压缩 DP**（`dp[mask][last]`，终点数 ≤ 8）求最优访问顺序 → 拼接各段路径。
- **接口**：`std::vector<Pos> multiGoalFind(const Grid&, Pos start, const std::vector<Pos>& goals);`
- **步骤**：对关键点两两调 `bfsFind`/`dijkstraFind` 建 `dist[i][j]` → 实现 bitmask DP 返回顺序+总长 → 按序拼接子路径（去掉重复端点）→ 自测 2~3 个终点小迷宫验证覆盖全部终点且最短。

#### 成员A：多终点交互（终端）
- **负责文件**：`src/core/Utils.cpp`（`printMaze` 支持多终点）、`src/main.cpp`
- **步骤**：`printMaze` 支持显示多个终点标记 `E` → 主循环用键盘输入坐标放置/移除终点 → 收集 `goals` 调 `multiGoalFind` → 打印整条路径，用 `.` 覆盖经过的格子。

**本周验收清单**
- [ ] 2~4 个终点能算出并画出覆盖全部终点的路径。
- [ ] 存在不可达终点时不崩溃、给出明确提示。
- [ ] 终端能清晰展示覆盖全部终点的路径。

---

### 第 6 周：动态障碍物

**本周目标**：运行中格子变化时路径自动更新，模拟实时重规划。

#### 成员B：重规划模块
- **负责文件**：`include/solver/Repath.h`、`src/solver/Repath.cpp`
- **方法**：方案A（推荐）事件触发式重算——格子一变就重新 A*；方案B D* Lite 增量式重规划为加分项。
- **接口**：
  ```cpp
  class RepathPlanner {
  public:
      RepathPlanner(const Grid& grid, Pos s, Pos g);
      void setCell(int r, int c, char ch);   // 改动并触发重算
      bool hasPath() const;
      const std::vector<Pos>& path() const;
  };
  ```
- **步骤**：持有 `_grid` 副本 + `_path` → `setCell` 更新格子并调内部 `_recompute()`（A*）→ `hasPath` 区分可达/阻断，阻断时清空路径 → 自测：直线路径中间放墙 → 断言绕行或报告不可达。

#### 成员A：终端交互重算
- **负责文件**：`src/main.cpp`
- **步骤**：主循环读取键盘输入「坐标 + 类型」切换 `' '`⇄`'#'` → 每次变化调 `planner.setCell` 并 `printMaze` 刷新 → `hasPath()==false` 时打印「当前无路径」。

**本周验收清单**
- [ ] 运行中改格子路径自动更新为合理新路径。
- [ ] 路径彻底阻断给出「不可达」提示不崩溃。
- [ ] 恢复受阻格子后路径自动复原。

---

### 第 7 周：自定义编辑 + 收尾

**本周目标**：收拢成完整可交付成品，用户可自定义迷宫。

#### 成员A：终端编辑模式
- **负责文件**：`src/main.cpp`
- **方法**：命令行交互编辑，无图形依赖。
- **交互约定**：按提示输入「命令 + 坐标」，如 `w 2 3` 画墙、`c 2 3` 删墙、`s 1 1` 设起点、`e 5 5` 设终点、`solve` 求解、`q` 退出。
- **步骤**：主循环读命令解析 → 更新 `Maze`/`Grid` 并立即 `printMaze` → 编辑结果存入 `RepathPlanner` 实现"编辑即求解" → `main` 提供 `edit`/`play` 两种模式入口。

#### 成员B：预设 + 入口整理
- **负责文件**：`include/generator/Presets.h`（或并入 `Generator.h`）、`src/main.cpp`
- **方法**：把常用配置做成预设表。
- **预设**：`tiny`11×11、`small`21×21（默认）、`medium`41×41、`large`101×101、`complex`更高连通度。
- **步骤**：定义 `struct Preset{ std::string name; unsigned w,h; };` + `lookup(name)` → 命令行统一解析 `--preset small`/`--size 31 31`/`--mode edit|play`（与第 4 周参数合并）→ 生成器加 `generate(complexity)` 通过额外打通随机墙控制复杂度 → 整理快捷键/参数说明。

#### 双方协作：联调收尾
- [ ] 完整走一遍「编辑迷宫 → 求解 → 切障碍 → 多终点」流程，逐个修 bug。
- [ ] 两人环境 `build.bat` 编译运行一致，无平台差异。
- [ ] 补 `README.md`：依赖、编译步骤、运行示例、快捷键/命令行说明。
- [ ] 录制演示视频（生成 → 求解 → 画墙重算 → 多终点）。
- [ ] `git tag v1.0` 发布，归档源码。

**本周验收清单**
- [ ] 能手动画迷宫、设起点终点、即时求解并高亮。
- [ ] 支持 tiny~large/complex 预设，命令行与界面入口清晰。
- [ ] 两人各自 clone 后一键编译、运行并复现演示。
- [ ] README 与实际一致，演示视频已录，`v1.0` 已发布。

---

## 五、交付物

- 可编译运行的 C++ 工程源码
- 随机迷宫生成（多种算法可选）
- BFS / Dijkstra / A* 三种求解算法
- 终端可视化模式（字符渲染 + 路径动画）
- 多终点最短路径、动态障碍物重规划、手动编辑
- README 使用说明与演示视频